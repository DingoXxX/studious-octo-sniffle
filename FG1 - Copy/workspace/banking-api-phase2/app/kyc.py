def verify_identity(user_id: str, document: str):
    # Simulate KYC verification (would be external API call in production)
    return {"user_id": user_id, "status": "verified", "document": document}
